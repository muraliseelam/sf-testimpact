Verbatim capture. Repository: https://github.com/trailheadapps/apex-recipes (CC0-1.0)
Commit under test: 2e2c1c3a8b197de06d6b709b877d1147c79fe26c  (chore: clean log)
Base (parent):     0238895a89f5a87e3c3242d266eb68974d096f80
Changed files:     force-app/main/default/classes/Platform Event Recipes/PlatformEventPublishCallback.cls 
Plugin version:    1.0.0
Captured:          2026-09-08T22:34:55Z

$ sf testimpact index
Indexed 204 files: 204 parsed, 0 unchanged
647 nodes, 1126 edges, 50 taints
Wrote .sf-testimpact/graph.json

--- shipped defaults (no .sf-testimpact.yml; entryPointPolicy: full) ---
$ sf testimpact analyze --base 0238895a89f5a87e3c3242d266eb68974d096f80
Changed files: 1  (0238895a89f5a87e3c3242d266eb68974d096f80...HEAD)

FALLBACK  entry-point-policy-full: QueueableChainingRecipes is an entry point (queueable), so its callers may live outside this repository and the graph’s inbound edges to it are incomplete.
          hint: `entryPointPolicy: widen` selects only the tests that reach an entry point of the same kind. Faster, but it assumes no unindexed caller reaches this class.
WIDEN     taint-domain-activated-apexType: Taint domain `apexType` activated because PlatformEventPublishCallback is impacted. Classes with unresolvable references into that domain are selected too.

Running the full suite (RunLocalTests). See the FALLBACK lines above.

--- .sf-testimpact.yml -> entryPointPolicy: widen ---
$ sf testimpact analyze --base 0238895a89f5a87e3c3242d266eb68974d096f80
Changed files: 1  (0238895a89f5a87e3c3242d266eb68974d096f80...HEAD)

WIDEN     entry-point-policy-widen: QueueableChainingRecipes is an entry point (queueable). Widening to 3 other entry point(s) of the same kind (queueable, trigger, rest, global, email), because an unindexed caller may reach the same subsystem through one of them.
WIDEN     taint-domain-activated-apexType: Taint domain `apexType` activated because PlatformEventRecipesTrigger is impacted. Classes with unresolvable references into that domain are selected too.

Selected 28 of 68 tests (58.8% skipped)
  AccountServiceLayer_Tests
  AccountTriggerHandler_Tests
  AuraEnabledRecipes_Tests
  BatchApexRecipes_Tests
  CalloutRecipes_Tests
  CanTheUser_Tests
  CollectionUtils_Tests
  CustomMetadataRecipes_Tests
  CustomRestEndpointRecipes_Tests
  FilesRecipes_Tests
  InboundEmailHandlerRecipes_tests
  InvocableMethodRecipes_Tests
  LDVRecipes_Tests
  MetadataTriggerHandler_Tests
  OrderAppMenu_Tests
  PSGAdvancedTestingRecipes
  PSGTestingRecipes
  PlatformCacheBuilderRecipes_Tests
  PlatformEventRecipesTriggerHandler_Tests
  PlatformEventRecipes_Tests
  QueueableChainingRecipes_Tests
  QueueableRecipes_Tests
  QueueableWithCalloutRecipes_Tests
  SOQLRecipes_Tests
  Safely_Tests
  ScheduledApexRecipes_Tests
  StripInaccessibleRecipes_Tests
  TestFactory
